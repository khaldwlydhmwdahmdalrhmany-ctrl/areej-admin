# النظام التسويقي — دليل التشغيل

## ١. تفعيل التتبّع (٥ دقائق)

`لوحة التحكم ← الإعدادات ← التتبّع والتحليلات`

| الحقل | من أين تحصل عليه | مطلوب؟ |
|---|---|---|
| `GTM-XXXXXXX` | tagmanager.google.com | **نعم — الأهم** |
| `G-XXXXXXXXXX` | analytics.google.com | اتركه فارغًا إن ربطت GA4 من داخل GTM |
| `AW-XXXXXXXXX` | Google Ads ← أدوات ← التحويلات | اختياري |
| Clarity ID | clarity.microsoft.com | اختياري — مجاني بالكامل |
| UET ID | ads.microsoft.com ← UET tag | اختياري |
| توثيق Search Console / Bing | لوحة كل منهما | لتوثيق ملكية الموقع |

بعد الحفظ، حدّث أي صفحة وتحقق بإضافة **Tag Assistant** من جوجل.

## ٢. تثبيت بكسلات المنصات — من GTM لا من الكود

الموقع يدفع أحداث **GA4 Ecommerce** القياسية إلى `dataLayer`:

| الحدث | متى يُطلق |
|---|---|
| `page_view` | كل انتقال بين الصفحات |
| `view_item` | فتح صفحة منتج |
| `add_to_cart` | الضغط على «أضف إلى السلة» |
| `begin_checkout` | الضغط على «إتمام الطلب» |
| `purchase` | بعد تسجيل الطلب (مع رقم الطلب والقيمة) |
| `generate_lead` | إرسال طلب فني أو صيانة عاجلة |

كل حدث يحمل `currency: "SAR"` و`value` و`items` بصيغة GA4، إضافة إلى
`traffic_source` و`traffic_medium`.

**لإضافة بكسل ميتا مثلًا:**
1. GTM ← Tags ← New ← Custom HTML → الصق كود ميتا الأساسي، المشغّل: All Pages
2. Tag جديد لحدث `Purchase`، المشغّل: Custom Event باسم `purchase`
3. مرّر القيمة عبر متغيّرات dataLayer: `value`, `currency`, `transaction_id`

نفس الطريقة لتيك توك وسناب وX. **لن تحتاج تعديل الكود مجددًا.**

## ٣. لوحة التحليلات الداخلية

`لوحة التحكم ← التحليلات`

تعمل **بلا أي إعداد خارجي** — البيانات تُسجَّل في قاعدتك مباشرة.

### لماذا لا نكتفي بـ Google Analytics؟

GA يخبرك بعدد الزيارات من كل قناة، لكنه لا يعرف الطلبات المحفوظة في قاعدة
بياناتك. لوحتنا تربط الاثنين: **كم طلبًا جاء فعلًا من سناب مقابل جوجل، وبكم ريال.**

### كيف يُحدَّد المصدر

بالترتيب:
1. وسوم **UTM** (`utm_source`, `utm_medium`, `utm_campaign`) — الأولوية المطلقة
2. **معرّفات النقر الإعلانية**: `gclid` و`gbraid` (جوجل) · `msclkid` (مايكروسوفت) ·
   `fbclid` (ميتا) · `ttclid` (تيك توك) · `ScCid` (سناب) · `twclid` (X)
3. **نطاق الإحالة** — google.com ⟵ بحث مجاني، instagram.com ⟵ سوشيال مجاني
4. **زيارة مباشرة** إن لم يوجد شيء

قاعدة الإسناد: **آخر مصدر غير مباشر يفوز**، محفوظ 30 يومًا. زيارة مباشرة لاحقة
لا تمحو الحملة التي جلبت العميل أصلًا.

### وسم روابط حملاتك

استخدم UTM دائمًا في إعلانات السوشيال:

```
https://areej-alnaqaa-admin.vercel.app/shop?utm_source=snapchat&utm_medium=paid_social&utm_campaign=summer_ro
```

## ٤. الخصوصية

- **لا نسجّل عنوان IP ولا بصمة الجهاز** — فقط المصدر والمسار ومعرّف جلسة عشوائي
- سياسة الخصوصية حُدّثت تلقائيًا بفقرة التتبّع (مطلب نظام حماية البيانات السعودي)
- لوحة التحكم `/admin` مستثناة من التتبّع

## ٥. ملاحظة عن الأرقام

اللوحة تعرض ما هو مسجَّل فعلًا فقط. **الفترات السابقة لتفعيل النظام ستظهر فارغة** —
لا نُقدّر ولا نستكمل بيانات غير موجودة. البيانات تبدأ بالتراكم من لحظة النشر.
