# الأداء والسيو — ما تغيّر ولماذا

## الأداء

### ١. التخزين المؤقت الموسوم (الأثر الأكبر)

**قبل:** كل صفحة `force-dynamic` — كل زيارة تفتح اتصالًا بقاعدة البيانات وتنتظر
النتيجة قبل عرض أي شيء. عند 500 منتج وزيارات متزامنة يصبح هذا عنق الزجاجة.

**بعد:** الصفحات تُخدَّم من الذاكرة، والكاش يُبطَل **عند التعديل فقط** لا بعد
مدة عشوائية. أي تغيير من لوحة التحكم يظهر خلال ثوانٍ.

| الوسم | يُبطَل عند |
|---|---|
| `products` | إضافة/تعديل/حذف منتج |
| `categories` | تعديل تصنيف |
| `banners` | تعديل بنر |
| `settings` | حفظ الإعدادات |

### ٢. حمولة السلة

`CartProvider` كان يستقبل **كل** بيانات المنتجات في كل صفحة. عند 500 منتج
تُرسل مئات الكيلوبايتات مع كل تنقّل. الآن يستقبل فهرسًا خفيفًا: المعرّف والاسم
والسعر والصورة فقط — وهو كل ما تحتاجه السلة.

### ٣. الترقيم التدريجي

صفحات المتجر والتصنيف والعروض تعرض **24 منتجًا** ثم زر «عرض المزيد».
عرض 500 منتج دفعة واحدة يعني 500 عقدة DOM و500 طلب صورة — يُجمّد الجوال.
الفلترة والفرز يعملان على القائمة كاملة، والعرض وحده مُقسّم.

### ٤. فهارس قاعدة البيانات

| الفهرس | يخدم |
|---|---|
| `idx_products_pub_cat_sort` | منتجات تصنيف مرتّبة (الاستعلام الأشيع) |
| `idx_products_pub_sort` | صفحة المتجر |
| `idx_products_discounted` | صفحة العروض |
| `idx_banners_placement` | بنرات كل صفحة |
| `idx_orders_created` | لوحة الطلبات |

كلها **جزئية** (`WHERE published IS NOT FALSE`) — أصغر وأسرع من الفهرس الكامل.
مطبّقة على Supabase ومضمّنة في الكود فتُطبَّق ذاتيًا.

### ٥. الصور

- ضغط وتحويل إلى WebP في المتصفح قبل الرفع
- `loading="lazy"` و`decoding="async"` على كل الصور خارج الشاشة الأولى

## السيو — كله تلقائي

### خريطة الموقع
`/sitemap.xml` تتولّد من قاعدة البيانات. **أي منتج أو تصنيف جديد يدخلها فور
إضافته** بلا خطوة يدوية. المنتجات المخفية مستبعدة تلقائيًا.

### robots.txt
`/robots.txt` يمنع فهرسة `/admin` و`/api`، ويستبعد الروابط الحاملة لوسوم
الحملات (`utm_`, `gclid`, `fbclid`) لمنع المحتوى المكرر، ويشير لخريطة الموقع.

### البيانات المنظّمة (Schema.org)

| النوع | أين | الفائدة |
|---|---|---|
| `Organization` | كل الصفحات | بطاقة الشركة في جوجل |
| `WebSite` | كل الصفحات | مربّع بحث داخل النتائج |
| `Product` + `Offer` | صفحة المنتج | **عرض السعر والتوفّر في نتائج البحث** |
| `BreadcrumbList` | المنتج والتصنيف | مسار التنقّل في النتيجة |
| `ItemList` | التصنيف | فهم بنية القائمة |
| `FAQPage` | المنتج | ظهور الأسئلة موسّعة |

### العناوين والأوصاف
تُشتق تلقائيًا من بيانات المنتج (الاسم، الماركة، السعر، الشحن والتركيب
المجاني)، وتُقصّ عند حدود الكلمات لا في منتصفها. لو كتبت `metaTitle` أو
`metaDescription` يدويًا فهما يفوزان.

### نصوص الصور البديلة
تُولّد من اسم المنتج وتصنيفه — مهمة للوصولية ولأرشفة الصور في جوجل.

## بعد النشر

1. **اضبط `NEXT_PUBLIC_SITE_URL`** في Vercel على الدومين الحقيقي بعد ربطه،
   وإلا ستشير الروابط الأساسية وخريطة الموقع إلى نطاق `vercel.app`.
2. أرسل `https://موقعك/sitemap.xml` إلى Google Search Console وBing Webmaster.
3. اختبر البيانات المنظّمة على `search.google.com/test/rich-results`.
