# دليل لوحة التحكم — التحديث الأخير

## ⚠️ الخطأ الحرج الذي كان يُسقط الموقع

**السبب:** مكوّن أيقونات التواصل كان مكوّن خادم (Server Component) لكنه يستخدم
`onMouseEnter`. عند خلو الروابط كان يُرجع `null` فلا يظهر الخطأ — وبمجرد إضافة
رابط واحد يحاول تمرير معالج حدث من مكوّن خادم، فيرمي Next.js خطأ. والفوتر موجود
في تخطيط الموقع كله، فينهار **كل** صفحة، ومعها زر «الانتقال للموقع» من لوحة التحكم.

**الحل:** التلوين صار بـ CSS خالص عبر متغيّر `--soc` بلا أي JavaScript.
فحصت بقية المشروع بحثًا عن نفس النمط — لا يوجد غيره.

## ضغط الصور تلقائيًا

عند رفع أي صورة (منتج / بنر / تصنيف) يضغطها المتصفح ويحوّلها إلى **WebP** قبل
الرفع، ويعرض لك كم وفّرت. الضغط في المتصفح لا على الخادم: أسرع للرفع على شبكة
الجوال، ولا يستهلك وقت تنفيذ دوال Vercel.

| النوع | الحد الأقصى | الجودة |
|---|---|---|
| منتج | 1200×1200 | 82% |
| بنر | 1920×1080 | 85% |
| أيقونة | 512×512 | 90% |

لن تُكبَّر صورة صغيرة، ولو خرج الملف أكبر من الأصل يُبقى الأصل كما هو.

## الشارات وصفحة العروض

18 شارة جاهزة في أربع مجموعات، مع إمكانية كتابة شارة مخصّصة.

**شارات المجموعة الترويجية** (عرض خاص، خصم محدود، عرض الأسبوع، تصفية، باقة
موفّرة، هدية مع المنتج) **تُدرج المنتج في صفحة العروض تلقائيًا** حتى بلا خصم
سعري — فبعض العروض حزم أو هدايا لا تخفيضات.

**التحكم في صفحة العروض:**
- `ترتيب الظهور` — رقم أصغر يظهر أولًا
- `تثبيت كصفقة الصدارة` — يضع المنتج في البطاقة الكبيرة أعلى الصفحة بدل الاختيار التلقائي

## الإخفاء بدل الحذف

**المنتجات:** زر «ظاهر / مخفي» في القائمة مباشرة، أو من نموذج التعديل.

> **أُصلح خلل مهم هنا:** حقل `published` كان موجودًا في قاعدة البيانات لكن
> **لا شيء يصفّي عليه** — أي أن المنتجات المخفية كانت تظهر في المتجر رغم إخفائها.
> الآن تُستبعد فعليًا من كل صفحات الموقع، وتظهر في لوحة التحكم وحدها.

**البنرات:** زر «نشط / متوقف» بنفس الطريقة.

## صفحة الطلبات

تبويبات حسب الحالة مع عدّاد لكل تبويب، بحث برقم الطلب أو الاسم أو الجوال، ثلاث
بطاقات ملخّص، ولكل طلب: زر نسخ الرقم، زر مراسلة العميل على واتساب مباشرة، ومصدر
الطلب التسويقي (جوجل / سناب / ميتا — مدفوع أم مجاني).

## إعدادات جديدة

| الإعداد | الأثر |
|---|---|
| رقم واتساب | يغيّر وجهة كل أزرار الطلب في الموقع |
| الشريط المتحرك | نصّه كان ثابتًا في الكود. افصل بين العبارات بالرمز ✦ — واتركه فارغًا لإخفائه |
| حد الشحن المجاني | يظهر في الشريط والسلة |
| إعلان مؤقت | شريط ذهبي بارز في كل الصفحات (إجازة، ظرف طارئ). فارغ = مخفي |

## تجاوب الجوال

- كل الشبكات ثنائية العمود صارت عمودًا واحدًا تحت 640 بكسل
- جدول المنتجات استُبدل ببطاقات — لا سحب أفقي إطلاقًا
- `overflow-x: hidden` على مستوى الصفحة يمنع الانزلاق الجانبي
- حجم خط الحقول 16 بكسل على الجوال — يمنع تكبير iOS التلقائي عند التركيز
- الصور والوسائط لا تتجاوز حاويتها

## ترحيلات قاعدة البيانات

مُطبَّقة مسبقًا على Supabase: `products.sortOrder` و`products.featuredOffer`،
وإعدادات المتجر الأولية. الكود يعيد تطبيقها ذاتيًا.
