# تقرير الأمان — ما وُجد وما أُصلح

## ⚠️ اقرأ هذا قبل النشر

بعد هذا التحديث، **لن يعمل تسجيل الدخول للوحة التحكم ما لم تُضبط هذه المتغيّرات في Vercel**.
هذا مقصود (فشل مغلق): الكود القديم كان يقبل الدخول بمفتاح افتراضي معروف.

`Vercel ← Project ← Settings ← Environment Variables`

| المتغيّر | القيمة |
|---|---|
| `SESSION_SECRET` | نص عشوائي طويل — استخدم المولَّد لك في المحادثة |
| `ADMIN_USERNAME` | اسم المستخدم |
| `ADMIN_PASSWORD` | كلمة مرور قوية — **غيّر `areej2026`** |

ثم أعد النشر (Redeploy) ليلتقط المتغيّرات الجديدة.

---

## ١. قاعدة البيانات — ما أبلغك به Supabase

**ما وجدته أوسع من الرسالة.** لم يكن الأمر غياب RLS فقط، بل إن دور `anon`
(المفتاح العام المخصص للمتصفحات) كان يملك على **كل** الجداول:
`SELECT, INSERT, UPDATE, DELETE, TRUNCATE`

شمل ذلك جدول `orders` الذي يحوي أسماء العملاء وأرقام جوالاتهم ومدنهم.

**ما نُفِّذ:**
1. تفعيل RLS على الجداول الستة
2. سحب كل الصلاحيات من `anon` و`authenticated` (دفاع في العمق — حتى لو أُضيفت
   سياسة خاطئة لاحقًا، غياب الصلاحية يمنع الوصول)
3. سحب `USAGE` على المخطط
4. `ALTER DEFAULT PRIVILEGES` — أي جدول جديد يُنشأ مستقبلًا يولد مقفلًا تلقائيًا

**لماذا لم يتعطّل الموقع:** التطبيق يتصل عبر `pg` مباشرةً بدور `postgres` وهو
**مالك الجداول**، ومالك الجدول يتجاوز RLS ما لم يُفعَّل `FORCE ROW LEVEL SECURITY`.
تركنا `FORCE` معطّلًا عمدًا. تحقّقت بعد التطبيق: التطبيق يقرأ الجداول كلها،
ودور `anon` محجوب تمامًا.

**التنبيهات المتبقية:** ستة بمستوى `INFO` نصّها "RLS مفعّل بلا سياسات".
هذا **صحيح ومقصود** — الموقع لا يستخدم PostgREST إطلاقًا، فغياب السياسات
يعني إغلاقًا كاملًا لا ثغرة. لا تُضف سياسات لتسكيت التنبيه، فذلك يفتح ما أغلقناه.

## ٢. ثغرة أخطر لم يذكرها Supabase

الـ middleware كان `matcher: ["/admin/:path*"]` — يحمي **الصفحات** فقط.
مسارات `/api` كانت مفتوحة بلا أي تحقّق. أي شخص يعرف رابط موقعك كان يستطيع:

- `DELETE /api/products/{id}` — حذف منتجاتك
- `PUT /api/settings` — تعديل إعداداتك، بما فيها **حقن سكربت عبر معرّف GTM**
- `POST /api/upload` — رفع ملفات إلى مساحتك

هذه أخطر من مشكلة القاعدة لأنها **لا تحتاج أي مفتاح** — الرابط العام يكفي.

**ما نُفِّذ:** كل `/api` صار محميًا بالجلسة، عدا ثلاثة يحتاجها الزائر فعلًا:

| المسار المفتوح | السبب |
|---|---|
| `POST /api/orders` | إتمام الطلب من السلة ونموذج الفني |
| `POST /api/track` | تسجيل الزيارة |
| `POST /api/admin/login` | تسجيل الدخول نفسه |

## ٣. تقوية تسجيل الدخول

**أ. تجاوز مصادقة كامل.** الكود كان:

```js
if (username !== validUser || password !== validPass) → رفض
```

لو لم يكن `ADMIN_USERNAME` مضبوطًا في البيئة لكان `undefined !== undefined`
يساوي `false`، فيمرّ طلب فارغ تمامًا ويُنشئ **جلسة مسؤول كاملة**.
الآن يرفض الخادم العمل أصلًا إن لم تُضبط بيانات الاعتماد.

**ب. مفتاح توقيع افتراضي.** `SESSION_SECRET` كان يعود إلى `"insecure-dev-secret"`.
من يعرف هذه القيمة (وهي في الكود) يستطيع تزوير جلسة مسؤول. الآن يرفض الإنتاج
العمل بلا مفتاح حقيقي.

**ج. مقارنة ثابتة الزمن** لكلمة المرور وتوقيع الجلسة — المقارنة العادية تتوقف
عند أول اختلاف، وفرق التوقيت يسرّب معلومات تساعد على التخمين بايتًا بايت.

**د. حدّ للمحاولات** — ٨ محاولات فاشلة لكل عنوان خلال ١٠ دقائق.

**هـ. رسالة خطأ موحّدة** — لا نكشف أي الحقلين كان خاطئًا.

## ٤. ما يجب أن تفعله بنفسك

- [ ] اضبط المتغيّرات الثلاثة أعلاه في Vercel **قبل** النشر
- [ ] غيّر كلمة مرور قاعدة البيانات من لوحة Supabase (كانت في ملف `.env` تداولناه)
- [ ] فعّل التحقق بخطوتين على حسابي Supabase وVercel
